# Speckit 命令系统

## 🎯 概述

Speckit是DocDDD项目的核心工作流命令系统，提供8个关键命令来实现从项目章程到代码实现的完整闭环。每个命令都对应文档驱动开发的一个关键阶段。

## 📋 命令列表

### 1. /constitution - 建立项目章程

**用途**：创建项目宪法/章程，确立项目的根本大法和价值基线

**语法**：
```bash
/constitution [选项]
```

**选项**：
- `--template` - 使用章程模板
- `--update` - 更新现有章程
- `--validate` - 验证章程完整性

**输出**：
- `project-docs/00-constitution/constitution.md`
- `project-docs/00-constitution/vision.md`
- `project-docs/00-constitution/principles.md`

**示例**：
```bash
# 创建新项目章程
/constitution --template

# 更新现有章程
/constitution --update

# 验证章程完整性
/constitution --validate
```

---

### 2. /specify - 定义功能规格

**用途**：将项目需求转化为精确的功能规格说明书

**语法**：
```bash
/specify [需求来源] [选项]
```

**选项**：
- `--from-user-stories` - 从用户故事生成规格
- `--from-requirements` - 从需求文档生成规格
- `--include-acceptance` - 包含验收标准
- `--format=markdown|html` - 输出格式

**输出**：
- `project-docs/01-specify/spec.md`
- `project-docs/01-specify/requirements.md`
- `project-docs/01-specify/user-stories.md`
- `project-docs/01-specify/acceptance-criteria.md`

**示例**：
```bash
# 基于用户故事生成规格
/specify --from-user-stories --include-acceptance

# 从现有需求生成规格
/specify --from-requirements
```

---

### 3. /clarify - 澄清模糊点

**用途**：识别并澄清规格文档中的模糊点和假设

**语法**：
```bash
/clarify [目标文档] [检查范围]
```

**选项**：
- `--target=spec|plan|all` - 澄清目标文档
- `--scope=requirements|assumptions|constraints` - 检查范围
- `--interactive` - 交互式澄清模式
- `--qa-log` - 生成问答记录

**输出**：
- `project-docs/02-clarify/clarifications.md`
- `project-docs/02-clarify/assumptions.md`
- `project-docs/02-clarify/edge-cases.md`
- `project-docs/02-clarify/qa-log.md`

**示例**：
```bash
# 澄清规格文档中的模糊点
/clarify --target=spec --interactive

# 全面澄清并生成问答记录
/clarify --target=all --qa-log
```

---

### 4. /plan - 生成技术方案

**用途**：将功能规格转化为可行的技术设计和实施计划

**语法**：
```bash
/plan [规格文档] [技术层次]
```

**选项**：
- `--architecture-only` - 仅生成架构设计
- `--include-api` - 包含API设计
- `--include-database` - 包含数据库设计
- `--tech-stack=[react|vue|angular]` - 技术栈选择

**输出**：
- `project-docs/03-plan/plan.md`
- `project-docs/03-plan/architecture.md`
- `project-docs/03-plan/technical-spec.md`
- `project-docs/03-plan/api-design.md`
- `project-docs/03-plan/database-design.md`

**示例**：
```bash
# 生成完整技术方案
/plan --tech-stack=react --include-api --include-database

# 仅生成架构设计
/plan --architecture-only
```

---

### 5. /tasks - 分解可执行任务

**用途**：将技术方案分解为具体的可执行任务和里程碑

**语法**：
```bash
/tasks [技术方案] [分解策略]
```

**选项**：
- `--by-module` - 按模块分解任务
- `--by-priority` - 按优先级分解任务
- `--include-dependencies` - 包含依赖关系
- `--timeline=[weeks|months]` - 时间线规划

**输出**：
- `project-docs/04-tasks/tasks.md`
- `project-docs/04-tasks/workflow.md`
- `project-docs/04-tasks/milestones.md`
- `project-docs/04-tasks/dependencies.md`

**示例**：
```bash
# 按模块分解任务，包含依赖关系
/tasks --by-module --include-dependencies

# 按优先级分解并规划时间线
/tasks --by-priority --timeline=weeks
```

---

### 6. /analyze - 验证一致性

**用途**：分析验证文档间的一致性、完整性和可追溯性

**语法**：
```bash
/analyze [分析类型] [目标范围]
```

**选项**：
- `--consistency` - 一致性分析
- `--traceability` - 可追溯性分析
- `--risk-analysis` - 风险分析
- `--impact-analysis` - 影响分析
- `--auto-fix` - 自动修复轻微问题

**输出**：
- `project-docs/05-analyze/consistency.md`
- `project-docs/05-analyze/traceability.md`
- `project-docs/05-analyze/risk-analysis.md`
- `project-docs/05-analyze/impact-analysis.md`

**示例**：
```bash
# 全面分析所有文档
/analyze --all

# 一致性分析并自动修复
/analyze --consistency --auto-fix
```

---

### 7. /checklist - 生成检查清单

**用途**：生成各类质量检查清单，确保交付质量

**语法**：
```bash
/checklist [检查类型] [阶段]
```

**选项**：
- `--quality` - 质量检查清单
- `--review` - 评审标准清单
- `--testing` - 测试检查清单
- `--deployment` - 部署检查清单
- `--stage=development|staging|production` - 目标阶段

**输出**：
- `project-docs/06-checklist/quality-checklist.md`
- `project-docs/06-checklist/review-criteria.md`
- `project-docs/06-checklist/testing-checklist.md`
- `project-docs/06-checklist/deployment-checklist.md`

**示例**：
```bash
# 生成生产部署检查清单
/checklist --deployment --stage=production

# 生成所有类型的检查清单
/checklist --all
```

---

### 8. /implement - 执行代码转换

**用途**：将文档转化为实际的代码实现和部署配置

**语法**：
```bash
/implement [任务范围] [执行模式]
```

**选项**：
- `--dry-run` - 试运行，显示将要执行的操作
- `--interactive` - 交互式执行模式
- `--from-tasks` - 基于任务列表执行
- `--target-module=[模块名]` - 目标模块
- `--skip-tests` - 跳过测试生成

**输出**：
- 代码文件
- `project-docs/07-implement/implementation-guide.md`
- `project-docs/07-implement/code-standards.md`
- `project-docs/07-implement/development-setup.md`
- `project-docs/07-implement/deployment-guide.md`

**示例**：
```bash
# 试运行查看将要执行的操作
/implement --dry-run

# 交互式执行特定模块
/implement --target-module=user-auth --interactive

# 基于任务列表执行
/implement --from-tasks
```

## 🔄 工作流集成

### 完整开发流程
```bash
# 1. 建立项目章程
/constitution --template

# 2. 定义功能规格
/specify --from-requirements --include-acceptance

# 3. 澄清模糊点
/clarify --target=spec --interactive

# 4. 生成技术方案
/plan --tech-stack=react --include-api

# 5. 分解可执行任务
/tasks --by-priority --include-dependencies

# 6. 验证一致性
/analyze --consistency

# 7. 生成检查清单
/checklist --quality --testing

# 8. 执行代码转换
/implement --dry-run
/implement --interactive
```

### 迭代开发流程
```bash
# 迭代开始
/analyze --impact-analysis
/clarify --target=changes
/plan --architecture-only
/tasks --by-module
/checklist --review
/implement --target-module=changed-module
```

## ⚙️ 配置选项

### 全局配置
```json
{
  "speckit": {
    "defaultTechStack": "react",
    "outputFormat": "markdown",
    "autoSave": true,
    "backupBeforeChange": true,
    "language": "zh-CN"
  }
}
```

### 命令别名
```bash
# 简化命令别名
alias cc="/constitution"
alias sp="/specify"
alias cl="/clarify"
alias pl="/plan"
alias ta="/tasks"
alias an="/analyze"
alias ch="/checklist"
alias im="/implement"
```

## 🔧 高级功能

### 批量处理
```bash
# 批量执行分析
/speckit batch-analyze --docs="spec,plan,tasks"

# 批量生成清单
/speckit batch-checklist --types="quality,testing,deployment"
```

### 模板定制
```bash
# 创建自定义模板
/speckit create-template --name="custom-spec" --type="specify"

# 使用自定义模板
/specify --template=custom-spec
```

### 插件扩展
```bash
# 安装插件
/speckit install plugin --name="advanced-qa"

# 使用插件功能
/clarify --plugin=advanced-qa
```

## 📊 输出报告

### 执行摘要
每个命令执行后都会生成执行摘要，包含：
- 执行时间
- 生成文档列表
- 警告和错误信息
- 后续建议

### 质量指标
- 文档完整性评分
- 一致性检查结果
- 可追溯性覆盖率
- 风险评估等级

---

*Speckit命令系统是DocDDD项目的核心工作流引擎，通过标准化的命令实现文档驱动开发的自动化和规范化。*